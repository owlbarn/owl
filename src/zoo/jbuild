(jbuild_version 1)

(library (
  (name owl_zoo)
  (public_name owl_zoo)
  (wrapped false)
  (modes (byte))
  (flags (:standard -safe-string))
  (libraries (
    owl
    ocaml-compiler-libs.toplevel
  ))
))
