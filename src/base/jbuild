(jbuild_version 1)

(copy_files# types/*)
(copy_files# optimise/*)
(copy_files# neural/*)

(library (
  (name owl_base)
  (public_name owl_base)
  (wrapped false)
  (flags (:standard -safe-string))
  (libraries (

  ))
))
