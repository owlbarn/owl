(jbuild_version 1)

(copy_files# maths/*)
(copy_files# optimise/*)
(copy_files# misc/*)

(library (
  (name owl_ocaml)
  (public_name owl_ocaml)
  (wrapped false)
  (flags (:standard -safe-string))
  (libraries (
    bigarray
  ))
))
