(jbuild_version 1)

(library (
  (name owl_optimise)
  (public_name owl_optimise)
  (wrapped false)
  (flags (:standard -safe-string))
  (libraries (
    owl_types
    owl_const
    owl_utils
    owl_log
  ))
))
