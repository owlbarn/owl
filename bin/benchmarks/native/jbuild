(jbuild_version 1)

(executables (
  (names        (test))
  (public_names (native_test))
  (package     owl_benchmarks)
  (libraries  (
    owl
    benchmarks_common
  ))
  (flags (:standard -safe-string))
  (modes      (native))
))
