(jbuild_version 1)

(executables (
  (names        (test map_eval))
  (public_names (native_test native_map_eval))
  (package     owl_benchmarks)
  (libraries  (
    owl
    benchmarks_common
  ))
  (flags (:standard -safe-string))
  (modes      (native))
))
