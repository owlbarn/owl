(jbuild_version 1)

(executables (
  (names        (test))
  (public_names (pure_test))
  (package     owl_benchmarks)
  (libraries  (
    benchmarks_common
  ))
  (flags (:standard -safe-string))
  (modes      (byte))
  (js_of_ocaml ((flags --pretty)))
))

(alias
  ((name install)
  (deps (test.bc.js))))
